

import React from 'react';

import {
  Modal,
  Form,
  Input,
} from 'antd';

// @Form.create();
class CategoryForm extends React.PureComponent{

  render(){
    const {state, modalVisible, handleAdd} = this.props;
    return (
      <Modal
        visible={true}
        
      >
        
      </Modal>
    )
  }
}